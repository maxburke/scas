#include "scas_base.h"
#include "scas_create.h"

int
scas_create_snapshot(const char *snapshot, const char *snapshot_root, const char *snapshot_server)
{
    UNUSED(snapshot);
    UNUSED(snapshot_root);
    UNUSED(snapshot_server);

    return 0;
}

